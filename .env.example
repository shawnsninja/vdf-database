# Supabase Configuration (example values)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Local database (Supabase CLI default)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# Staging database via Supabase pooler (example only; put real value in .env.local)
# REMOTE_DATABASE_URL=postgresql://postgres.your-project-ref:your-password@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# Application Settings
NEXT_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# Development Settings
NODE_ENV=development
